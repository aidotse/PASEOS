name: Docker Image CI

on:
  push:
    branches: [ "create_docker_image" ]
  pull_request:
    branches: [ "create_docker_image" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
